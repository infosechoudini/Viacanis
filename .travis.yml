---
language: rust
rust:
    - nightly
sudo: true

matrix:
  include:
    - os: windows
    - os: linux
      dist: bionic
cache:
  - apt
  - cargo

jobs:
  include:
      - if: os = windows      
        script: 
          - cd agent
          - cargo build
          - cd ../controlcenter
          - cargo build
      - if: os = linux
        script:
          - cd ./controlcenter
          - cargo build
