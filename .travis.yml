---
language: rust
rust:
    - nightly
sudo: true
cache:
  - apt
  - cargo

jobs:
  include:
      - os: windows
      script: 
        - cd agent
        - cargo build
        - cd ../controlcenter
        - cargo build
      - os: linux
        dist: Focal
        script:
          - cd ../controlcenter
          - cargo build
