---
language: rust
rust:
    - nightly
sudo: true

cache:
  - apt
  - cargo

matrix:
  include:
    - os: windows
      - if: os = windows      
        script: 
          - cd agent
          - cargo build
          - cd ../controlcenter
          - cargo build
    - os: linux
      - if: os = linux
        script:
          - cd ./controlcenter
          - cargo build
